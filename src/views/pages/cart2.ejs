<div class="c"></div>

<script>
    "<% for (i = 0; i < cartItems.length; i++) { %>"
    var code = `<div class="small-container cart-page">
    <table>
        <tr>
            <th>"<%= cartItems[i].product %>"</th>
            <th><%= cartItems[i].quantity %></th>
            <th><%= cartItems[i].price * cartItems[i].quantity %></th>
        </tr> 

        <tr>
            <td>
                <div class="cart-info">
                    <img src="// pull from DB">
                    <div>
                        <p>description (from DB)</p>
                        <small>price: (from DB)</small>
                        <br>
                        <a href="">Remove product</a>
                    </div>
                </div>
            </td>
            <td>
                <input type="number" value="1">
            </td>
            <td>  
                <%= cartItems[i].price * cartItems[i].quantity %>
            </td>
        </tr>
    </table>
</div>`;
        document.querySelector(".c").innerHTML += code;
        "<% } %>"
</script>