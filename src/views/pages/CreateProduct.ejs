<div class="container">
  <header>Create New Product:</header>

  <form action="#">
    <div class="form first">

        <span class="title">Product Details</span><br>
        <form action="/api/create_product" method="POST" id="myForm">
          <!-- 2 column grid layout with text inputs for the first and last names -->
          <div class="fields">
            <div class="input-field">
              <label>Product name</label>
              <input name="Product_Name" type="text" placeholder="Enter Product name" required />
            </div>
            <div class="input-field">
              <label>Category</label>
              <input autocomplete="off" list="categories" name="category" placeholder="Choose the Category" required>
              <datalist id="categories">
                <% for (i=0; i < category.length; i++) { %>
                  <option value="<%= category[i].id  %>">
                    <%= category[i].name %>
                  </option>
                  <% } %>
              </datalist>
            </div>
       </form>
   
         <!--Brand Input-->
        
          <div class="input-field">
            <label>Brand</label>
            <input autocomplete="off" list="brands" name="brand" placeholder="Choose the Brand" required>
            <datalist id="brands">
            <% for (j=0; j < brand.length; j++) { %>
            <option value="<%= brand[j].id  %>">
              <%= brand[j].name %>
            </option>
            <% } %>
            </datalist>
          </div>
        </div>

      <div class="fields">
      <div class="input-field">
        <label>Rating</label>
        <input name="rating" type="number" max="5" min="1" placeholder="Enter Rating" required>
      </div>

      <div class="input-field">
        <label>Image 1</label>
        <input name="image" type="text" placeholder="Enter Image 1" required>
      </div>

      <div class="input-field">
        <label>Image 2</label>
        <input name="image" type="text" placeholder="Enter Image 2" required>
      </div>

      <div class="input-field">
        <label>Image 3</label>
        <input name="image" type="text" placeholder="Enter Image 3" required>
      </div>

      <div class="input-field">
        <label>Image 4</label>
        <input name="image" type="text" placeholder="Enter Image 4" required>
      </div>

      <!-- Size input -->
      <div class="input-field">
        <label>Size</label>
        <input name="size" type="number" placeholder="Enter the Shoes Size" required>
      </div>

      <!-- Shoes Quantity input -->
      <div class="input-field">
        <label>Shoes Quantity</label>
        <input name="quantity2" type="number" placeholder="Enter the Shoes Quantity" required>
      </div>

      <!-- Size Quantity input -->
      <div class="input-field">
        <label>Size Quantity</label>
        <input name="quantity" type="number" placeholder="Enter the Size Quantity" required>
      </div>

      <!-- Price input -->
      <div class="input-field">
        <label>Price</label>
        <input name="price" type="number" placeholder="Enter the Price" required>
      </div>

      <div class="input-field">
        <label>Description</label>
        <textarea name="description"></textarea>
      </div>

    

    <button type="submit" class="btn btn-primary btn-block mb-4">Create </button>
  </div>
  </div>
</form>
</div>


<script>
  var token = "EAAHXdUsF3nQBAFvaJxAHVGGSFp1rpguFRbH1j4q3iYqfR29oslJxjW4VLlu02IMtjTZAzurvHRBeLbrto6xOZBKudUIqzTCE2nm8gYSotaUkaXcUXhOYney36DCv8SxAGyZCP7weeRkgEOiVQvFLEx5lP59DhwJB1RzmUl11sE9F1ZCZC9e7YB3PaZA4YW5hPtZCQxvZAGbsODgDK8fksJG1";

  $(document).ready(function() {
    $('#myForm').submit(function(e) {
      e.preventDefault();
      $.ajax({
        url: '/api/create_product',
        data: $(this).serialize(),
        type: 'POST',
        success: function(data) {
          var image = 'localhost:3000/product-page?id=' + data.id;
          $.ajax({
            url: 'https://graph.facebook.com/104491532469853/photos',
            data: {
              message: "Just in! " + data.Product_name + " is now available for purchase. Click the link below to purchase." + "\n" + image,
              url: data.image[0],
              access_token: token
            },
            type: 'POST',
            success: function(data) {
              window.location.href = "/shop";
            }
          });
        },
      });
    });
  });
</script>






